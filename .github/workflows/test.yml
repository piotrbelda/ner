name: Test Project
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Get code
        uses: actions/checkout@v3
      - name: List internal directories
        run: ls -la
  test-2:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: List internal directories
        run: ls -la
